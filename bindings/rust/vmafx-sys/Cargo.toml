[package]
name = "vmafx-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to libvmaf (VMAFX fork)"
license = "BSD-3-Clause"
links = "vmaf"
build = "build.rs"

[build-dependencies]
bindgen = "0.69"

[dev-dependencies]
