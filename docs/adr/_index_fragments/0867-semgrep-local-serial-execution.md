| [ADR-0867](0867-semgrep-local-serial-execution.md) | Set `require_serial: true` on the `semgrep-local` pre-commit hook: pre-commit's default parallel fan-out spawns `cpu_count()` semgrep-core processes that each `io_uring_queue_init`, collectively exhausting `RLIMIT_MEMLOCK` (default 8 MB) on workers with `>=16` cores; surfaced as opaque `exit code 2` with `--quiet` swallowing the trace (PR #331, 744-file VMAFx rebrand). | Accepted | 2026-05-30 | ci, pre-commit, lint, developer-experience, fork-local |
