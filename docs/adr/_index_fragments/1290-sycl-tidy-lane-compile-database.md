| [ADR-1290](1290-sycl-tidy-lane-compile-database.md) | The tidy ratchet owns the compilation database it measures. meson emits the SYCL feature TUs as `CUSTOM_COMMAND` rules (`icpx -fsycl`), so `write-compile-commands.py` — native `c`/`cpp_COMPILER` rules only — never saw them and `make tidy-ratchet LANE=sycl` never ran `gen-sycl-compile-commands.py`: the lane measured **0** SYCL feature TUs against the 18 the generator produces, and `tidy-baseline-sycl.json` recorded an empty backend. Because a lane only counts `NOLINT` markers in files it measures, 15 uncited markers (14 SYCL, 1 Metal) were invisible to every lane and the GPU backlog read 6 where the tree-wide figure was 21. Both ratchet targets now expand a per-lane `TIDY_RATCHET_COMPDB_<lane>` hook between the native export and the measurement, pinned by `scripts/ci/tests/test_tidy_ratchet_sycl_compdb.py`; the two undocumented build-dir preconditions (`-Db_lto=false` per ADR-1172, and a build dir outside the repository so generated `<build>/src/*.json.c` stay out) are documented in the Makefile and `docs/development/ci.md`. All three GPU baselines re-measured with 0 compile failures. | Accepted | ci, sycl, cuda, hip, lint, agents |
