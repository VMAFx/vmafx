| [ADR-1106](1106-hip-motion-v2-mirror-reflect101-correction.md) | HIP `integer_motion_v2` mirror corrected from `2*size-idx-1` to reflect-101 `2*size-idx-2`, matching the CPU/CUDA/SYCL twins. Supersedes ADR-0377's incorrect claim that the `-1` form matched CPU/CUDA (all three use `-2`; identical call sites, so it was a one-pixel high-boundary divergence — same class as ADR-1103's HIP VIF fix). Also adds the missing `shape[d] <= 0` guard in `build_input_tensor` (DNN infer path). HIP-only; no CPU golden impact. | Accepted | 2026-06-13 | hip, gpu, motion-v2, parity, boundary, bug-fix, fork-local |
