| [ADR-1282](1282-mypy-python-version-follows-requires-python.md) | mypy's `python_version` follows `requires-python`, and is therefore `3.14`. The stale `3.10` pin did not just model a language version nothing runs: mypy refuses to parse a PEP 695 `type` statement below 3.12 and numpy's bundled `__init__.pyi` has one, so the `ai/src/` pass aborted with "errors prevented further checking" and checked zero source files in any checkout with numpy installed. At 3.14 that pass completes and 60 pre-existing findings become visible. Measured against a merge base carrying the same value, the raise introduces 0 findings of its own; the debt was hidden, not created. Nothing is suppressed to absorb it. | Proposed | ci, python, tooling, hooks, ai, fork-local |
