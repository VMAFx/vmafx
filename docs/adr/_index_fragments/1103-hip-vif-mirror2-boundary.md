| [ADR-1103](1103-hip-vif-mirror2-boundary.md) | Fix integer_vif_hip boundary condition: all filter-loop reads used clamp_i (replicate-edge), disagreeing with CPU PADDING_SQ_DATA and the CUDA twin's two-bounce mirror-reflect. The mismatch produced max |HIP−CPU| ≈ 0.0018 per VIF scale (places~2.75) on the Netflix src01 pair, violating the ADR-0214 places=4 gate. Replaces clamp_i with mirror2_i in all six filter-loop boundary reads; confirmed places~6 (max 1e-6) on gfx1030 wave32 hardware across all 48 src01 frames. Tightens test_hip_vif_parity.c tolerance from 1e-3 to 1e-4. | Accepted | 2026-06-13 | hip, vif, parity, boundary, correctness, fork-local |
