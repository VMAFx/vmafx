| [ADR-1125](1125-vmafx-tune-go-port-integration.md) | Reconciling seven independently-written vmafx-tune Go ports into one tree. Six packages were invented two or three times over under the same import path; each collision is resolved on evidence rather than merge order — `pkg/codecadapter` on Python-parity assertion count (16 vs 3 vs 4, with both registries verified to register the identical 19 codecs), the rest on superset. `internal/pyjson` is the deliberate exception and is kept twice as `pyjson` (mirrors `json.dumps`, bare `NaN`/`Infinity` tokens) and `pyjsonstrict` (mirrors `vmaftune.jsonio.dumps_strict`, non-finite → `null`), because they mirror different Python entry points with different non-finite contracts. Where APIs disagreed the receiving package grew the missing seam rather than rewriting consumers. Completes the Python CLI shadow: `root.go` registers no redirect stubs and `TestStubSubcommands` is inverted into `TestNoStubSubcommandsRemain`. Integration surfaced three latent defects — a bare `corpus.jsonl` gitignore pattern silently excluding a committed test fixture, `* text=auto` normalising CRLF out of the benchmark CSV goldens, and a cgo `-L` at a nonexistent path silently linking the distro libvmaf (upstream 3.2.0, zero `vmaf_dnn_*` symbols). | Accepted | 2026-08-30 | go, vmaf-tune, python-sunset, integration, fork-local |
