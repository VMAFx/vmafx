| [ADR-1264](1264-hip-scaffold-enosys-contract.md) | The HIP scaffold posture (`enable_hipcc=false`, the default) reports `-ENOSYS` and nothing else, and HIP parity tests recognise it at both observation points — `vmaf_use_feature()` and the frame submit. Four tests failed on a default HIP build instead of skipping: `float_vif_hip.c` and `integer_psnr_hvs_hip.c` called a kernel-submit helper with `rb == NULL`, whose NULL check is its first statement, so they always returned `-EINVAL` and their `return -ENOSYS` was unreachable; and `test_hip_speed_singular_parity.c` checked `-ENOSYS` only at registration although its extractor reports it from `extract()`. Default build 173 ok / 4 fail to 177 ok / 0 fail; with real kernels 183 ok / 0 fail. | Proposed | hip, testing, scaffold, gpu, fork-local |
