| [ADR-1155](1155-tools-upstream-mirror-rework.md) | Rework CLI and tools translation units (`vmaf.cpp`, `cli_parse.cpp`, `y4m_input.c`, `vmaf_bench.c`, `cli_parse.h`) in place to fork lint standards, eliminating 348 clang-tidy warnings down to 0. C translation units preserve `NULL` and suppress `modernize-use-nullptr` with file-scoped brackets citing ADR-1138 to maintain MSVC `/std:clatest` Windows portability. Resolves suspected dead twin `core/tools/cli_parse.c` under ADR-1153 precedent by proving zero unique behavior and deleting it while rewiring test targets to `cli_parse.cpp`. Verifies byte-identical CLI behavior across a 16-test matrix against pristine master. | Accepted | tools, cli, clang-tidy, lint, dead-twin, portability, c23, cpp23 |
