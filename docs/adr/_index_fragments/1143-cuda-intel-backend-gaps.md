| [ADR-1143](1143-cuda-intel-backend-gaps.md) | CUDA and Intel SYCL backend gap closure: wires `vmaf_gpu_dispatch_env_get` into CUDA and SYCL dispatch selectors, adds honest warning and direct fallback for unimplemented CUDA graph capture (`VMAF_CUDA_DISPATCH=graph`), implements `VMAF_FORCE_BACKEND` and `backend` options in Python harness `ExternalProgramCaller`, scopes CI GPU pytest legs away from Netflix CPU golden assertions, removes dead CUDA files (`adm_decouple.cu`, `resolution_dispatch.c`/`.h`), adds informative error log before returning `-ENOSYS` on `_WIN32` in SYCL dmabuf import, and corrects stale TensorRT EP documentation. | Accepted | 2026-09-02 | cuda, sycl, ci, dispatch, python, docs |
