| [ADR-1107](1107-threaded-multi-prev-ref-extractor-starvation.md) | Fix multi-PREV_REF starvation in `threaded_read_pictures_batch`: the batch struct-copied a single `prev_ref` snapshot into the first temporal extractor and zeroed the shared snapshot, so a 2nd PREV_REF extractor (e.g. `motion_v2`, which co-schedules `motion`) saw NULL prev_ref and returned -EINVAL every frame — failing 100% of `--threads N` extractions involving motion_v2 (incl. the K150K retrain corpus). Fix: each PREV_REF extractor takes its own `vmaf_picture_ref()`; snapshot released once at `unref:`. Regression test `test_batch_two_prev_ref_extractors`. Scores unchanged. | Accepted | 2026-06-13 | core, threading, feature-extractor, refcount, bug-fix, fork-local |
