<<<<<<< HEAD
| [ADR-0594](0539-hip-ssimulacra2-blur-fp-contract-off.md) | Per-kernel `hip_cu_extra_flags` dict in `core/src/meson.build` (first entry `ssimulacra2_blur: ['-ffp-contract=off']`) mirrors the CUDA `cuda_cu_extra_flags --fmad=false` carve-out so the recursive Gaussian IIR `n2*sum - d1*prev` step keeps IEEE-754 add/mul ordering on hipcc / amdclang++ device code and `ssimulacra2` HIP scores stay inside the ADR-0214 places=2 parity gate vs CPU. Fall-through (`.get(name, [])`) is byte-identical for every kernel not listed. Verified on AMD gfx1036 — `ssimulacra2` / `integer_ssim` / `integer_ms_ssim` HIP scores match CPU to display precision. | Accepted | hip, build, ssimulacra2, numerics, fork-local |
=======
| [ADR-0594](0594-hip-ssimulacra2-blur-fp-contract-off.md) | Per-kernel `hip_cu_extra_flags` dict in `core/src/meson.build` (first entry `ssimulacra2_blur: ['-ffp-contract=off']`) mirrors the CUDA `cuda_cu_extra_flags --fmad=false` carve-out so the recursive Gaussian IIR `n2*sum - d1*prev` step keeps IEEE-754 add/mul ordering on hipcc / amdclang++ device code and `ssimulacra2` HIP scores stay inside the ADR-0214 places=2 parity gate vs CPU. Fall-through (`.get(name, [])`) is byte-identical for every kernel not listed. Verified on AMD gfx1036 — `ssimulacra2` / `integer_ssim` / `integer_ms_ssim` HIP scores match CPU to display precision. | Accepted | hip, build, ssimulacra2, numerics, fork-local |
>>>>>>> 24bb5daf89 (docs: post-merge-train sweep — VMAFx + core/ path refs, ADR index, state.md)
