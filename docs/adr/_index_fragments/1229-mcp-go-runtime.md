| [ADR-1229](1229-mcp-go-runtime.md) | Make the Go `vmafx-mcp` binary the MCP server everywhere and deprecate the 15,091-line Python `mcp-server/vmaf-mcp/` package. Both implement the same fifteen tools — verified by comparing `tools/list` output name-by-name and required-argument-by-required-argument — and the Go binary was already built and installed at `/usr/local/bin` in every container image while going unused. The dev entrypoint and Containerfile switch over and the `pip install -e` layer drops; the Python tree is deprecated rather than deleted in the same change, so a regression can be diagnosed against a working reference. | Accepted | mcp, go, python, container, dependencies, fork-local |
