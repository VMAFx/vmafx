| [ADR-1228](1228-upstream-ab-perf-milestone.md) | Establish a recurring "faster than upstream, and still exact" milestone: `testdata/bench_upstream_ab.py` A/Bs the fork's CPU path against upstream Netflix/vmaf at a pinned tag, reporting speedup and score delta, with parity gating the speed number. Triggered before every release, on any CUDA / ROCm / oneAPI bump, on a new upstream tag, and on any performance PR; hardware-generation retuning (arch lists, occupancy assumptions, ISA dispatch) is part of the milestone rather than opportunistic work. First run: the fork measures **0.989x** vs `v3.2.0` — parity, not a win — and surfaced a ~5e-6 score divergence that the golden gate's `places=4` hides. | Accepted | performance, benchmarking, cuda, sycl, hip, process, fork-local |
