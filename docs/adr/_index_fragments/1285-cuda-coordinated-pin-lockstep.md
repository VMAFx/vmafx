| [ADR-1285](1285-cuda-coordinated-pin-lockstep.md) | CUDA is a coordinated pin, not an image tag: one release is named in sixteen places across seven files in seven spellings, and Renovate's base-image manager could see two of them, which is why the `nvidia/cuda` tags-only pull request #1487 was structurally unmergeable against `check-base-image-single-source.sh` rule 3. A custom manager plus a `CUDA release (coordinated pin)` group put every Renovate-writable site in one branch; `scripts/ci/check-cuda-pin-lockstep.py` checks all sixteen, derives and `--write`s the five Renovate cannot express (`$cudaMajorMinor`, the `cuda-toolkit-NN-N` apt names, the OCI description label), and fails on any CUDA release literal in a spelling it does not know. That residual sweep found the OCI label, which was in no inventory of the pin. `CUDA_APT_PACKAGE` had no consumer and no drift check; it has one now. | Accepted | ci, build, cuda, renovate, fork-local |
