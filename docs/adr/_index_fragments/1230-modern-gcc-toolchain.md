| [ADR-1230](1230-modern-gcc-toolchain.md) | Stop freezing the CI gcc at the distro's version while clang (22, from apt.llvm.org) and meson (from PyPI) are deliberately pulled forward for the tree's C23/C++26 target: the clang-tidy lanes install gcc-15 from ubuntu-toolchain-r/test. Also make the ratchet diagnosable — it now records `cc_version` (clang-tidy parses against the C compiler's system headers, so counts depend on gcc as much as on clang-tidy) and keeps every `path:line:col: [check]` diagnostic in the report artifact, while the baseline stays a reviewable count file. Prompted by PR #1392, where a `+1` the ratchet reported could not be reproduced on gcc-15/16 and `Tidy Changed` called the same file clean. Requires a CI-side baseline regeneration. | Accepted | ci, build, tooling, clang-tidy, fork-local |
