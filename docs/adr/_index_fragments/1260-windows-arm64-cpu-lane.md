| [ADR-1260](1260-windows-arm64-cpu-lane.md) | Add the `Windows ARM64 MSVC` lane to `libvmaf-build-matrix.yml`: a native Windows-on-ARM64 build of libvmaf with the ARM64-hosted MSVC toolset on `windows-11-vs2026-arm`, a PE machine check (`0xAA64`) and the meson `fast` suite on ARM64 silicon. First lane to compile `core/src/feature/arm64/` and the `#if ARCH_AARCH64` branches with `cl.exe`. Advisory until the maintainer promotes it; amends ADR-1259's matrix table. CPU only: CUDA 13.3.1 ships no `windows-arm64` packages, the 13.4 bump is a separate item. With it, `test_ciede_neon.c`'s guard-page probe gains a Windows path (`VirtualAlloc` + SEH) and the arm64 strict-FP carve-outs pass `/fp:precise` to MSVC instead of `-ffp-contract=off`. | Proposed | ci, build, arm64, windows, simd, fork-local |
