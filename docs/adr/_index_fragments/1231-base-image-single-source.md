| [ADR-1231](1231-base-image-single-source.md) | Define every container base image in one root-level `build-config.env` instead of pinning each by hand at the point of use. Dockerfiles take bases as `ARG`s whose defaults mirror the config, so a plain `docker build` still works and CI can override with `--build-arg`; `scripts/ci/check-base-image-single-source.sh` fails on drift and `make base-images-sync` rewrites the mirrors. Same authority-plus-drift-check shape as the default-model gate. Closes four Debian 12 pins and two Ubuntu 24.04 CUDA pins, and brings four pins hidden inside `COPY --from=<image>` under the gate by converting them to named stages. `vmafx-controller` gains an explicit `USER 65532:65532` (its old `cc-debian12` pin lacked `:nonroot`, so it ran as root). ROCm and oneAPI keep self-closing 24.04 exemptions pending PR #1386 and the oneAPI 2026.1 restructure. | Accepted | build, ci, docs, security, fork-local |
