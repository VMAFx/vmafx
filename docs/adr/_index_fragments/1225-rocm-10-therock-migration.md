| [ADR-1225](1225-rocm-10-therock-migration.md) | Migrate every ROCm consumer to 10.0.0, installed from the digest-pinned `rocm/dev-ubuntu-24.04:10.0.0-full` image rather than the `repo.radeon.com` apt channel that AMD froze at 7.2.4 when ROCm moved to TheRock at 7.14. Containers copy a pruned `/opt/rocm` (19 GB → 5.5 GB, gated by a hipcc smoke compile); CI streams the same subset out of the registry via `scripts/ci/install-rocm-from-image.sh` instead of a 29 GB `docker pull`. Drops `HSA_OVERRIDE_GFX_VERSION=10.3.0` — ROCm 10 supports `gfx1036` natively, so aliasing it to `gfx1030` would contradict the compiled code objects. Fixes the `node-rocm` runtime copy set, which ROCm 10's widened `libamdhip64.so` closure had silently outgrown. | Accepted | hip, rocm, build, ci, container, dependencies, fork-local |
