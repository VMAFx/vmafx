| [ADR-1302](1302-nonfinite-scores-fail-the-frame.md) | A sweep of all 466 `vmaf_feature_collector_append*` sites found [ADR-1301](1301-speed-nonfinite-score-fails-frame.md)'s defect at twelve more places, and several are worse than SpEED's was: a NaN is published not as an implausible 1000.0 but as the *best possible score* — `ssimulacra2.c:916` returns 100.0, `adm.c:379` a perfect AIM of 1.0, and `convert_to_db()` in both `float_ssim.c` and `float_ms_ssim.c` returns `max_db` from a guard whose own comment was written for this exact situation (`NaN >= 1.0` is false, so the NaN falls past it into `MIN(NaN, max_db)`). `adm`, `vif` and `ssim` are core VMAF sub-features, so nothing downstream could tell a measurement from a non-measurement. The existing `brisque.c` / `y_funque_plus.c` convention is extended to four of them — guard with `isfinite` before the comparison that would launder the value, warn, return `-EINVAL` — placed before the frame's first append so a frame fails atomically. Verified rather than argued: the Netflix golden gate is `271 passed, 12 skipped` before and after, and the clang-tidy ratchet is unmoved at 8/0/2/2. Five sites are held in Issue #1526 because the guard pushes their enclosing function past the 60-line limit and needs a helper extracted first. | Accepted | metrics, correctness, fork-local |
