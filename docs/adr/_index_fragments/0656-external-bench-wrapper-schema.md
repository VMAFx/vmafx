| [ADR-0656](0656-external-bench-wrapper-schema.md) | External-bench fork wrappers now emit registry competitor keys (`fork-fr-regressor`, `fork-nr-metric`) in `summary.competitor` so `compare.py` can validate and aggregate the fork's own FR/NR competitors instead of skipping them as schema mismatches. Shell-wrapper smoke tests use a fake `vmaf-tune` binary to pin the contract without installing external competitors. | Accepted | ai, testing, tooling, fork-local |
