| [ADR-1116](1116-autotune-prefilter-control-plane.md) | vmaf-tune autotune prefilter control plane — new `filter_adapters/` family (sibling to `codec_adapters/`) with a `FilterAdapter` Protocol + `pelorus_deband.py` hard-coding the 10 frozen knobs from the Pelorus ADR-0110 contract and emitting `-vf pelorus_deband_vulkan=...`; new `vmaf-tune prefilter` subcommand drives a JOINT Optuna TPE search over the deband knob space + CRF (reusing the `fast.py` TPESampler study) with VMAF as the oracle, returning recommended strengths + CRF + per-probe VMAF. vmafx stays Vulkan-free (emits the string, scores the output); the live `deband→encode→score` loop is gated behind `pelorus_filter_available()`. Unit-tested (adapter emission/validation, search-space, mocked subcommand loop); live encode untested here (no pelorus-enabled ffmpeg build). | Accepted | 2026-06-14 | vmaf-tune, autotune, pelorus, filter-adapters, control-plane, optuna, fork-local |
