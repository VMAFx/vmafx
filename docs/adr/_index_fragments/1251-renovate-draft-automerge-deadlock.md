| [ADR-1251](1251-renovate-draft-automerge-deadlock.md) | Renovate opens automerge-eligible and security bumps ready for review. `draftPR: true` from PR #1411 plus the ADR-0679 rule that the required aggregator fails every draft meant no bump could ever merge: twenty PRs stalled for five days, two of them security fixes. `draftPR: false` now applies to the six `packageRules` that set `automerge: true` and to `vulnerabilityAlerts`; everything else still opens as a draft. | Accepted | ci, dependencies, renovate, merge-train, fork-local |
