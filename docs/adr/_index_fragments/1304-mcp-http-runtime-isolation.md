| [ADR-1304](1304-mcp-http-runtime-isolation.md) | The Python MCP server and HTTP adapter use a narrow shared scoring protocol instead of importing each other. The canonical CLI installs one default adapter, while an embedded `run_http_server(runtime=...)` binds its injected adapter to that server's aiohttp application without replacing process-global state, so overlapping servers cannot cross-contaminate path-validation or scoring policy. | Proposed | mcp, python, architecture, concurrency |
