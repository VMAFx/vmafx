| [ADR-1301](1301-speed-nonfinite-score-fails-frame.md) | Every SpEED backend bounded its score with a less-than comparison before emitting it — `MIN(x, max)` on the CPU, an inline ternary on the CUDA/HIP/SYCL twins, a `CLIP` macro in `speed_chroma_cuda.c` — and every comparison against NaN is false, so all fourteen sites published a NaN as `speed_*_max_val`: a plausible 1000.0 in place of a computation that produced no number, and invisible to the parity harness's own `isfinite()` assertion (`-Inf` escaped unclamped instead). It was reachable two ways: `matrix_qr_decomposition()` in `speed.c` divided the Householder vector by its own norm with no zero guard, so a deflated minor produced `0.0f/0.0f` — a guard `speed_internal.c` has always had, making the CPU reference the one backend that could manufacture the NaN — and the entropy's `log2(L*var + sigma_nn)` takes a negative argument when an ill-conditioned fp32 solve returns the quadratic form `var` slightly negative, which at chroma eigenvalues of 1e3–1e4 needs only -1e-5. `speed_internal_clamp_score()` is now the single implementation for all fourteen: check finiteness, warn naming extractor/feature/frame/value, return `-EINVAL`, extending the `brisque.c` / `y_funque_plus.c` convention rather than inventing a second one. The QR guard closes the source, and the CPU temporal path finally honours `speed_temporal_max_val`, which it declared and documented but never applied while every twin did. | Accepted | metrics, speed, correctness, cross-backend, fork-local |
