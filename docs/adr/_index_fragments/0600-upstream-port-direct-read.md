| [ADR-0600](0567-upstream-port-direct-read.md) | Port Netflix upstream `30a6e2a8d` — add `USE_DIRECT_READ` compile flag to CLI tools that eliminates the intermediate `video_input_ycbcr` buffer and per-frame `memcpy` by reading directly into preallocated `VmafPicture` planes via a new `fetch_into_vmaf_picture` vtable slot. Extends `vidinput.h/c`, `yuv_input.c`, `y4m_input.c`, and `vmaf.c`; Y4M direct path gated on `convert == y4m_convert_null`. Flag is opt-in; old path retained under `#else`. | Accepted | upstream-port, performance, tools, cli, build |
