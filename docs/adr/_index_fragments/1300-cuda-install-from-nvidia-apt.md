| [ADR-1300](1300-cuda-install-from-nvidia-apt.md) | The Linux CUDA legs installed the toolkit through `Jimver/cuda-toolkit`, whose own release table decided which CUDA versions the fork could pin: v0.2.36 tops out at 13.3.1 with no 13.4.x entry, so Renovate's 13.4.1 branch sat unmergeable for two weeks — raising `CUDA_VERSION` asked the action for a release it cannot serve, leaving it failed `check-cuda-pin-lockstep`. `scripts/ci/install-cuda-toolkit.sh` installs `cuda-nvcc-<series>` and `cuda-cudart-dev-<series>` from NVIDIA's own apt repository instead, reading `CUDA_APT_PACKAGE` from `build-config.env` and deriving the distro tag from `/etc/os-release`, so a release is pinnable the day NVIDIA publishes it and the CI legs share the dev container's source. Removing the action drops `renovate.json`'s now-unmatched `cuda:` matchString and adds `CUDA_PATH_V<major>_<minor>` to the lockstep gate as a sixth derived spelling — the one case where the release is baked into a variable name, which left two sites reading `CUDA_PATH_V13_3` after a `--write` had moved every other spelling. | Accepted | ci, cuda, gpu, dependencies, fork-local |
