| [ADR-1287](1287-cli-tool-unbounded-loop-ceilings.md) | Bound the fork CLI tools' open-ended read and retry loops: `vmaf-perShot`'s frame scan stops at `UINT32_MAX` (the width of the counter its shot records store) and reports `EFBIG`; `vmaf_vpl`'s `DecodeFrameAsync` retry stops after 60 000 attempts, derived from the 60 s ceiling the same function already gives `MFXVideoCORE_SyncOperation` at the 1 ms back-off it already used. Records what each ceiling does and does not guarantee, and the two user-visible failure modes they open. | Accepted | tools, cli, reliability, docs |
