| [ADR-1236](1236-version-single-source-tree.md) | Single-source package versions across the tree and unify Python dependencies under `python/pyproject.toml`. `python/requirements.txt` is mechanically derived via `scripts/ci/check-python-requirements-single-source.sh`, and `setup.py` omits duplicate `install_requires`. Renovate ignores `python/requirements.txt` to prevent dual-PR race conditions. Resolves cross-tree version disagreements for numpy (>=2.5.2), scipy (>=1.18.1), matplotlib (>=3.11.1), pyarrow (>=25.0.1), and synchronizes package versions. | Accepted | build, ci, python, packaging, renovate |
