| [ADR-0903](0903-wire-codecov-upload.md) | Wire `codecov/codecov-action@v6.0.1` (SHA-pinned) into both `Coverage Gate` jobs in `tests-and-quality-gates.yml`. Uses fork-aware OIDC (no `CODECOV_TOKEN` secret); `fail_ci_if_error: false` so Codecov outages do not double-gate the gcovr threshold check; flag-tagged `cpu` vs `gpu` to mirror the two-job split. Closes the gap PR #383 documented ("Codecov badge intentionally NOT added: no Codecov upload step exists in any workflow"). | Accepted | 2026-05-30 | ci, coverage, codecov, observability, fork-local |
