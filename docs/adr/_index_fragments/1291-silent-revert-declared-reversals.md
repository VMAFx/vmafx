| [ADR-1291](1291-silent-revert-declared-reversals.md) | A reversal an accepted ADR mandates is declared in `scripts/ci/silent-revert-allowlist.json` — ADR, detector, commit undone, exact paths, and a regex every line of the finding must match — not by the per-PR `reverts:` line, which exempts a whole run including findings nobody has looked at. Repairs two detectors in the same pass: `reverse-hunk` no longer reports files the branch deliberately deletes (a pure-addition target commit made every removal look like a rewind, and `dropped` already separates a deletion the branch asked for from one it did not), and `resurrected` now requires the line to be text the target once held and lost, as its own docstring always defined it. | Accepted | ci, docs, agents |
