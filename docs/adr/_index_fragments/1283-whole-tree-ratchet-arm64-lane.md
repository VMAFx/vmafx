| [ADR-1283](1283-whole-tree-ratchet-arm64-lane.md) | ADR-1142 bounds the whole tree with the clang-tidy ratchet, but all four lanes (`cpu`, `cuda`, `hip`, `sycl`) build x86: the 32 translation units only an aarch64 build compiles (20 under `core/src/feature/arm64/`, `core/src/arm/cpu.c`, and 11 `test_*_neon.c` parity tests) had no compile command anywhere, and the fast `Tidy Changed` job excludes that directory outright — `vif_neon.c` carried 36 findings nobody ever saw. Adds an `arm64` cross lane: compile database from the in-tree `build-aux/aarch64-linux-gnu.ini`, clang-tidy pointed at the same `--target` / `--sysroot`, baseline measured into `scripts/ci/tidy-baseline-arm64.json`. Parity with the GPU lanes; promoting any non-`cpu` lane to a required context stays a separate decision. | Proposed | ci, clang-tidy, lint, ratchet, simd, arm64, fork-local |
