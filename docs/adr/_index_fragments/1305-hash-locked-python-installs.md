| [ADR-1305](1305-hash-locked-python-installs.md) | Python dependencies across workflows, Dockerfiles, setup scripts, and Makefile are hash-locked with cryptographic SHA-256 digests via manifest-driven locks compiled by a reviewed uv release, enforcing `--require-hashes` repository-wide. Pure sdist documentation consumers use pinned build-isolation dependencies (`setuptools`, `wheel`) under `--no-build-isolation`, `dev-linters.txt` targets Python 3.12 portability, executable Makefile pip commands are brought under contract checking, and SLSA builder tag requirements are preserved. | Accepted | security, dependencies, supply-chain, python, ci |
