| [ADR-1263](1263-hip-platform-macro-single-source.md) | `__HIP_PLATFORM_AMD__` is declared once, by `hip_deps` in `core/src/hip/meson.build`, instead of by a hand-written `#define` at the top of eight HIP host sources. The macro is in the implementation-reserved `__` namespace, so each of those files tripped `cert-dcl37-c` as soon as a PR touched it, and the build-side define sat inside the `if not hip_runtime_dep.found()` fallback only — a ROCm shipping `hip-lang.pc` relied entirely on the in-file copies. Both definitions were textually identical, which C permits silently, so nothing ever failed. Verified by a full HIP-lane build with every in-file define removed. | Proposed | hip, build, meson, warnings, fork-local |
