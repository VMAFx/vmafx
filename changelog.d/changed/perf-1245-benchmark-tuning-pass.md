- **CAMBI AVX2 anti-dithering vectorization**: `anti_dithering_filter()` in `core/src/feature/cambi.c` now dispatches to a bit-exact 16-lane AVX2 vector kernel (`anti_dithering_filter_avx2()`) on x86-64, achieving a 3.7× speedup on 8-bit luma pre-filtering (ADR-1237).
- **SpEED SIMD QR matrix multiplication**: `si_mat_mul()` in `core/src/feature/speed_internal.c` now dispatches through `speed_matmul_avx512` / `speed_matmul_avx2` / `speed_matmul_scalar`, yielding a 4–5× speedup on host-side QR factorisations for GPU SpEED twins (ADR-1237).
- **Threaded GPU flush & extractor pooling**: updated `batch_extractor_skip()`, `read_pictures_should_skip()`, and `flush_non_temporal_cpu_extractors()` to recognize `VMAF_FEATURE_EXTRACTOR_HIP` and `VMAF_FEATURE_EXTRACTOR_METAL`, and ensured `flush_context_threaded()` drains `gpu_pending` before flushing temporal extractors, resolving `-EINVAL` (-22) errors when running HIP with `--threads N` (ADR-1237).
- **Benchmark harness fixes**: `testdata/bench_all.sh` now prefers `/opt/intel/oneapi/setvars.sh` (2026.0) ahead of legacy installs to prevent symbol resolution mismatches, and Test 2 points to the canonical 1080p checkerboard fixtures (`checkerboard_1920_1080_10_3_0_0.yuv` and `checkerboard_1920_1080_10_3_1_0.yuv`).
