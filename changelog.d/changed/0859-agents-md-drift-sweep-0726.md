- **docs**: sweep all 44 AGENTS.md files for staleness post-ADR-0700
  (`libvmaf/` → `core/` path rename) and post-ADR-0726 (Vulkan backend
  drop). Updates headings, directory trees, inline path refs, twin-update
  tables, and rebase-invariant §13 entries. Fixes committed merge-conflict
  marker in `core/AGENTS.md`. Adds deprecation headers to retained Vulkan
  source directories (ADR-0859).
