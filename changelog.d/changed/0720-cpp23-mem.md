- **refactor(core):** Convert `mem.c` to `mem.cpp` (C++23) — adds `[[nodiscard]]`
  to `aligned_malloc`, drops the `_POSIX_C_SOURCE` feature-test macro, preserves
  the public C ABI via `extern "C"` guards in `mem.h`. No behaviour change;
  zero-on-allocate and alignment-pass-through semantics are preserved exactly.
  Also fixes a pre-existing `meson setup` failure caused by stale `test_ansnr_simd`
  references in `core/test/meson.build` left over from the ansnr feature drop.
  (ADR-0720)
