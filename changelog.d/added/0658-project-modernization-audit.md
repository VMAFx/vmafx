- Add `scripts/dev/project_modernization_audit.py`, a read-only JSON/Markdown
  scanner that ranks repo modernization gaps, local state rows, smoke-model
  registry entries, and large AI script-family clusters.
