vmaf_tiny v2/v3/v4/v5 training stats JSON files now include shared
`run_provenance` metadata for the trainer entrypoint, argv, parsed
hyperparameters, parquet input(s), checkpoint target, and stats target.
