- **The README carries its full badge set again, and a banner.** The rc1
  documentation pass had cut it from 22 badges to four, leaving no signal for
  the lint, security, FFmpeg, Go and Rust lanes, the toolchain versions, the GPU
  and SIMD coverage, the OpenSSF Scorecard or the licence split. All of it is
  back, every badge pointing at a workflow or endpoint that resolves, plus the
  "Why VMAFx" comparison and "Backends at a glance" sections the same pass
  removed.
