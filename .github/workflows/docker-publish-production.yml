# docker-publish-production — build, push, sign, and SBOM the VMAFX production image.
#
# Triggers:
#   - publication of a release-please draft (`release.published`)
#   - workflow_dispatch at an existing published tag for recovery
#
# Produces:
#   ghcr.io/vmafx/vmafx:<tag>           (cpu, amd64+arm64)
#   ghcr.io/vmafx/vmafx:latest          (same)
#   ghcr.io/vmafx/vmafx:<tag>-cuda13    (amd64 only — CUDA not portable to arm64)
#   ghcr.io/vmafx/vmafx:<tag>-rocm7     (amd64 only)
#   ghcr.io/vmafx/vmafx:<tag>-oneapi2025 (amd64 only)
#

# Post-push steps: cosign keyless sign + syft SBOM (CycloneDX JSON) via cosign attest.
# ADR-0698: vmafx-production-dockerfile

name: docker-publish-production

on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing published vX.Y.Z tag to recover idempotently"
        required: true

# Workflow-level least-privilege; jobs that push/sign/attest opt in below.
permissions:
  contents: read

# Prevent duplicate signing/push runs for the same tag.
# cancel-in-progress: false — a partial publish (some images built, some not)
# is worse than a queued duplicate run completing after the first finishes.
concurrency:
  group: docker-publish-production-${{ github.event.release.tag_name || github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: vmafx/vmafx
  # Validation below proves this tag, GITHUB_REF, GITHUB_SHA, and the published
  # GitHub release all identify the same immutable source commit.
  PUBLISH_TAG: ${{ github.event.release.tag_name || github.event.inputs.tag }}

jobs:
  validate-release:
    name: Validate tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.release.outputs.tag }}
      is-latest: ${{ steps.release.outputs.is_latest }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ env.PUBLISH_TAG }}
          persist-credentials: false
      - name: Reject source/tag mismatches
        id: release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_IS_PRERELEASE: ${{ github.event.release.prerelease || false }}
        run: |
          # ADR-1201: release candidates are published as images too, so a
          # prerelease is no longer rejected. The tag and the release flag must
          # AGREE: an -rc.N tag must be marked prerelease and a final tag must
          # not. The dangerous case is an RC published as a stable release,
          # which would make it eligible for the `latest` moving tag.
          case "$PUBLISH_TAG" in
            *-rc.*) tag_is_rc=true ;;
            *)      tag_is_rc=false ;;
          esac
          if [[ "$RELEASE_IS_PRERELEASE" != "$tag_is_rc" ]]; then
            echo "ERROR: $PUBLISH_TAG has prerelease=$RELEASE_IS_PRERELEASE but the tag says rc=$tag_is_rc" >&2
            exit 1
          fi
          expected_ref="refs/tags/$PUBLISH_TAG"
          if [[ "$GITHUB_REF" != "$expected_ref" ]]; then
            echo "ERROR: run this workflow at $expected_ref, not $GITHUB_REF" >&2
            exit 1
          fi
          scripts/release/verify-release-version.sh "$PUBLISH_TAG"
          if [[ "$(git rev-parse HEAD)" != "$GITHUB_SHA" ]]; then
            echo "ERROR: workflow source SHA $GITHUB_SHA does not match checked-out tag" >&2
            exit 1
          fi
          release_json="$RUNNER_TEMP/github-release.json"
          gh api "repos/$GITHUB_REPOSITORY/releases/tags/$PUBLISH_TAG" > "$release_json"
          if [[ "$(jq -r '.tag_name' "$release_json")" != "$PUBLISH_TAG" || \
                "$(jq -r '.draft' "$release_json")" != "false" || \
                "$(jq -r '.prerelease' "$release_json")" != "$tag_is_rc" || \
                "$(jq -r '.published_at != null' "$release_json")" != "true" ]]; then
            echo "ERROR: $PUBLISH_TAG must name a published release with prerelease=$tag_is_rc" >&2
            exit 1
          fi
          echo "tag=$PUBLISH_TAG" >> "$GITHUB_OUTPUT"
          # ADR-1151 — the `latest` moving tag used to be gated on
          # `github.event_name == 'release'`, so the documented recovery
          # dispatch (docs/development/release.md) republished the versioned
          # tag but left `latest` pointing at whatever digest the broken
          # original run had pushed. Resolve it from the repository's actual
          # newest published release instead, which makes the guard identical
          # on both trigger paths.
          # ADR-1201 belt-and-braces: a release candidate must never take the
          # `latest` moving tag. GitHub's /releases/latest already excludes
          # prereleases, so an -rc.N tag can never match below -- but `latest`
          # pointing at an RC is severe and silent for every consumer that pulls
          # it, so the suffix is also refused explicitly rather than relying on
          # one endpoint's semantics staying that way.
          case "$PUBLISH_TAG" in
            *-rc.*)
              echo "is_latest=false" >> "$GITHUB_OUTPUT"
              echo "::notice::$PUBLISH_TAG is a release candidate; never tagged 'latest'"
              exit 0
              ;;
          esac
          latest_tag="$(gh api "repos/$GITHUB_REPOSITORY/releases/latest" \
            --jq '.tag_name' 2>/dev/null || true)"
          if [[ "$latest_tag" == "$PUBLISH_TAG" ]]; then
            echo "is_latest=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_latest=false" >> "$GITHUB_OUTPUT"
            echo "::notice::$PUBLISH_TAG is not the newest release (${latest_tag:-none}); skipping the 'latest' tag"
          fi

  # -----------------------------------------------------------------------
  # Build + push the CPU (default) multi-arch image
  # -----------------------------------------------------------------------
  build-cpu:
    name: Publish CPU image
    needs: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: release-publish
    permissions:
      contents: read
      packages: write    # push to GHCR
      id-token: write    # cosign keyless OIDC
      attestations: write

    outputs:
      digest: ${{ steps.push.outputs.digest }}
      image-ref: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.validate-release.outputs.tag }}
          submodules: recursive
          persist-credentials: false

      - name: Set up QEMU (cross-arch emulation)
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a  # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.PUBLISH_TAG }}
            type=raw,value=latest,enable=${{ needs.validate-release.outputs.is-latest == 'true' }}

      - name: Build and push (amd64 + arm64)
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: docker/Dockerfile.production
          target: cli
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            VMAF_BUILD_JOBS=4

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Sign image (keyless OIDC via Sigstore)
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Attest build provenance (GitHub native)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2

      - name: Generate SBOM (CycloneDX JSON)
        run: |
          syft \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}" \
            --output cyclonedx-json \
            --file sbom-cpu.cdx.json

      - name: Attach SBOM via cosign attest
        run: |
          cosign attest --yes \
            --predicate sbom-cpu.cdx.json \
            --type cyclonedx \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-cpu
          path: sbom-cpu.cdx.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # Smoke-test: pull the just-pushed image and verify --version exits 0
  # -----------------------------------------------------------------------
  smoke-test:
    name: Smoke-test CPU image
    needs: build-cpu
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      packages: read

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Verify signature before pull
        run: |
          cosign verify \
            --certificate-identity \
              "https://github.com/VMAFx/vmafx/.github/workflows/docker-publish-production.yml@refs/tags/${PUBLISH_TAG}" \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-cpu.outputs.digest }}"

      - name: Pull and run --version
        run: |
          docker pull \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-cpu.outputs.digest }}"
          docker run --rm \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-cpu.outputs.digest }}" \
            --version
          echo "Smoke test passed: --version exited 0"

  # -----------------------------------------------------------------------
  # GPU variant: CUDA 13 (amd64 only)
  # -----------------------------------------------------------------------
  build-cuda13:
    name: Publish CUDA 13 image
    needs: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment: release-publish
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      digest: ${{ steps.push.outputs.digest }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.validate-release.outputs.tag }}
          submodules: recursive
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata (cuda13 tag suffix)
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.PUBLISH_TAG }}-cuda13

      - name: Build and push (amd64 only)
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: docker/Dockerfile.production-gpu
          target: final-cuda13
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Sign image (keyless OIDC)
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Attest build provenance (GitHub native)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2

      - name: Generate SBOM + attest
        run: |
          syft \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}" \
            --output cyclonedx-json \
            --file sbom-cuda13.cdx.json
          cosign attest --yes \
            --predicate sbom-cuda13.cdx.json \
            --type cyclonedx \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-cuda13
          path: sbom-cuda13.cdx.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # GPU variant: ROCm 7 (amd64 only)
  # -----------------------------------------------------------------------
  build-rocm7:
    name: Publish ROCm 7 image
    needs: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment: release-publish
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      digest: ${{ steps.push.outputs.digest }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.validate-release.outputs.tag }}
          submodules: recursive
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.PUBLISH_TAG }}-rocm7

      - name: Build and push
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: docker/Dockerfile.production-gpu
          target: final-rocm7
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign + sign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Sign image
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Attest build provenance (GitHub native)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2

      - name: Generate SBOM + attest
        run: |
          syft \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}" \
            --output cyclonedx-json \
            --file sbom-rocm7.cdx.json
          cosign attest --yes \
            --predicate sbom-rocm7.cdx.json \
            --type cyclonedx \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-rocm7
          path: sbom-rocm7.cdx.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # GPU variant: oneAPI 2025 (amd64 only)
  # -----------------------------------------------------------------------
  build-oneapi2025:
    name: Publish oneAPI 2025 image
    needs: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 90
    environment: release-publish
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      digest: ${{ steps.push.outputs.digest }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.validate-release.outputs.tag }}
          submodules: recursive
          persist-credentials: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.PUBLISH_TAG }}-oneapi2025

      - name: Build and push
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: docker/Dockerfile.production-gpu
          target: final-oneapi2025
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign + sign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Sign image
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Attest build provenance (GitHub native)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2

      - name: Generate SBOM + attest
        run: |
          syft \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}" \
            --output cyclonedx-json \
            --file sbom-oneapi2025.cdx.json
          cosign attest --yes \
            --predicate sbom-oneapi2025.cdx.json \
            --type cyclonedx \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-oneapi2025
          path: sbom-oneapi2025.cdx.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # GPU runtime smoke: no device is required for the version-only probe.
  # -----------------------------------------------------------------------
  smoke-gpu:
    name: Smoke-test GPU images
    needs:
      - build-cuda13
      - build-rocm7
      - build-oneapi2025
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: read

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Verify signatures before pull
        run: |
          for digest in \
            "${{ needs.build-cuda13.outputs.digest }}" \
            "${{ needs.build-rocm7.outputs.digest }}" \
            "${{ needs.build-oneapi2025.outputs.digest }}"; do
            cosign verify \
              --certificate-identity \
                "https://github.com/VMAFx/vmafx/.github/workflows/docker-publish-production.yml@refs/tags/${PUBLISH_TAG}" \
              --certificate-oidc-issuer https://token.actions.githubusercontent.com \
              "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${digest}"
          done

      - name: Pull and run each GPU entrypoint
        run: |
          for digest in \
            "${{ needs.build-cuda13.outputs.digest }}" \
            "${{ needs.build-rocm7.outputs.digest }}" \
            "${{ needs.build-oneapi2025.outputs.digest }}"; do
            image="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${digest}"
            docker pull "${image}"
            docker run --rm "${image}" --version
          done

  # (ADR-0726: Vulkan backend removed; build-vulkan job deleted)

  # -----------------------------------------------------------------------
  # MCP server variant (cpu base, amd64 + arm64)
  # -----------------------------------------------------------------------
  build-server:
    name: Publish MCP server image
    needs: validate-release
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: release-publish
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    outputs:
      digest: ${{ steps.push.outputs.digest }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          ref: ${{ needs.validate-release.outputs.tag }}
          submodules: recursive
          persist-credentials: false

      - name: Set up QEMU
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a  # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.PUBLISH_TAG }}-server

      - name: Build and push
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          file: docker/Dockerfile.production
          target: server
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign + sign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Sign image
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Attest build provenance (GitHub native)
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Install syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@3ad7283483fc7af8ff2b4ea19663c2d5ca935e26  # v0.24.2

      - name: Generate SBOM + attest
        run: |
          syft \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}" \
            --output cyclonedx-json \
            --file sbom-server.cdx.json
          cosign attest --yes \
            --predicate sbom-server.cdx.json \
            --type cyclonedx \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ steps.push.outputs.digest }}"

      - name: Upload SBOM as workflow artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: sbom-server
          path: sbom-server.cdx.json
          retention-days: 90

  # -----------------------------------------------------------------------
  # Server smoke: verify the signature, Python 3.14 interpreter, and CLI.
  # -----------------------------------------------------------------------
  smoke-server:
    name: Smoke-test MCP server image
    needs: build-server
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      packages: read

    steps:
      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2
        with:
          cosign-release: "v3.1.3"

      - name: Verify signature before pull
        run: |
          cosign verify \
            --certificate-identity \
              "https://github.com/VMAFx/vmafx/.github/workflows/docker-publish-production.yml@refs/tags/${PUBLISH_TAG}" \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-server.outputs.digest }}"

      - name: Pull and verify Python + live vmaf-mcp HTTP server
        run: |
          image="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}@${{ needs.build-server.outputs.digest }}"
          docker pull "${image}"
          docker run --rm --entrypoint /venv/bin/python "${image}" --version
          docker run --rm --entrypoint /venv/bin/vmaf-mcp "${image}" --help
          smoke_token="$(openssl rand -hex 32)"
          container_id="$(docker run --detach \
            --publish 127.0.0.1:18080:8080 \
            --env "VMAFX_MCP_HTTP_TOKEN=${smoke_token}" \
            "${image}")"
          cleanup() {
            docker logs "${container_id}"
            if docker inspect "${container_id}" >/dev/null 2>&1; then
              docker rm --force "${container_id}"
            fi
          }
          trap cleanup EXIT
          for attempt in $(seq 1 30); do
            if curl --fail --silent --show-error \
              --oauth2-bearer "${smoke_token}" \
              http://127.0.0.1:18080/healthz; then
              break
            fi
            if ! docker inspect "${container_id}" >/dev/null 2>&1; then
              echo "server container exited before becoming healthy" >&2
              exit 1
            fi
            if [[ "${attempt}" -eq 30 ]]; then
              echo "server did not become healthy" >&2
              exit 1
            fi
            sleep 1
          done
          curl --fail --silent --show-error \
            --oauth2-bearer "${smoke_token}" \
            http://127.0.0.1:18080/readyz \
            | grep --extended-regexp '"status"[[:space:]]*:[[:space:]]*"ready"'

  # -----------------------------------------------------------------------
  # Summary job — depends on all build jobs so CI shows a single status
  # -----------------------------------------------------------------------
  all-images:
    name: Images published
    if: always()
    needs:
      - build-cpu
      - smoke-test
      - build-cuda13
      - build-rocm7
      - build-oneapi2025
      - smoke-gpu
      - build-server
      - smoke-server
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check results
        run: |
          if [[ "${{ needs.build-cpu.result }}" != "success" ]]; then
            echo "build-cpu failed"
            exit 1
          fi
          if [[ "${{ needs.smoke-test.result }}" != "success" ]]; then
            echo "smoke-test failed"
            exit 1
          fi
          if [[ "${{ needs.smoke-server.result }}" != "success" ]]; then
            echo "smoke-server failed"
            exit 1
          fi
          if [[ "${{ needs.smoke-gpu.result }}" != "success" ]]; then
            echo "smoke-gpu failed"
            exit 1
          fi
          # The GPU + server variants used to be echoed and never gated, so they
          # could fail on every publish without anyone noticing. That is how
          # docker/Dockerfile.production-gpu kept a CUDA 12.0 pin, an
          # intel/oneapi-basekit:2026.0 reference to an image that does not
          # exist, and a ROCm 6.4 runtime against a 7.2.4 build toolchain.
          # They gate now.
          fail=0
          for job in build-cuda13 build-rocm7 build-oneapi2025 build-server; do
            case "$job" in
              build-cuda13)      r="${{ needs.build-cuda13.result }}" ;;
              build-rocm7)       r="${{ needs.build-rocm7.result }}" ;;
              build-oneapi2025)  r="${{ needs.build-oneapi2025.result }}" ;;
              build-server)      r="${{ needs.build-server.result }}" ;;
            esac
            if [[ "$r" != "success" ]]; then
              echo "::error title=image build failed::$job = $r"
              fail=1
            else
              echo "$job = $r"
            fi
          done
          [[ "$fail" -eq 0 ]] || exit 1
          echo "all image variants passed"
