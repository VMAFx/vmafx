---
# SPDX-License-Identifier: BSD-3-Clause-Plus-Patent
# Copyright 2026 Lusoris
#
# templates/operator-rbac-@KIND_LOWER@.yaml — ClusterRole rules for the @KIND@ controller.
#
# Generated by /add-k8s-resource. Tight default — no delete, no wildcard verbs.
# Add additional rules only with an ADR justifying the verb extension.
#
# ADR-0714: vmafx-operator kubebuilder skeleton + CRDs.
{{- if and .Values.operator.enabled .Values.operator.controllers.@KIND_LOWER@.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "vmafx.fullname" . }}-operator-@KIND_LOWER@
  labels:
    {{- include "vmafx.labels" . | nindent 4 }}
rules:
  - apiGroups: [vmafx.dev]
    resources: [@PLURAL@]
    verbs: [get, list, watch, create, update, patch]
  - apiGroups: [vmafx.dev]
    resources: [@PLURAL@/status]
    verbs: [get, update, patch]
  - apiGroups: [vmafx.dev]
    resources: [@PLURAL@/finalizers]
    verbs: [update]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "vmafx.fullname" . }}-operator-@KIND_LOWER@
  labels:
    {{- include "vmafx.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "vmafx.fullname" . }}-operator-@KIND_LOWER@
subjects:
  - kind: ServiceAccount
    name: {{ include "vmafx.fullname" . }}-operator
    namespace: {{ .Release.Namespace }}
{{- end }}
