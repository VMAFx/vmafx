# Copyright 2026 Lusoris
# SPDX-License-Identifier: BSD-3-Clause-Plus-Patent
#
# vmaf_mcp/tools/@NAME@.py — Python handler for the @NAME@ MCP tool.
#
# Generated by /add-mcp-tool @NAME@. Keep the response shape byte-identical to
# the Go handler in cmd/vmafx-mcp/impl_@NAME@.go; the parity contract lives in
# ADR-0703 and is checked by scripts/mcp/parity-check.sh.

from __future__ import annotations

from typing import Any

TOOL_NAME = "@NAME@"

INPUT_SCHEMA: dict[str, Any] = {
    "type": "object",
    # TODO(@NAME@): list required keys and per-key schema.
    "required": [],
    "properties": {},
}


def handle(arguments: dict[str, Any]) -> dict[str, Any]:
    """Execute the @NAME@ tool.

    Errors MUST be raised so the MCP server can set isError=True on the
    response payload (see project_mcp_iserror_must_be_true memory entry).
    Returning a dict with an error field but no exception silently
    mis-signals success.
    """
    # TODO(@NAME@): validate arguments against INPUT_SCHEMA.
    # TODO(@NAME@): invoke vmaf / vmaf-tune subprocess and parse stdout.
    raise NotImplementedError(f"{TOOL_NAME}: handler stub — implement me")
