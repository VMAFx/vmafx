# MCP tool — `@NAME@`

> Generated by `/add-mcp-tool @NAME@`. Replace every `TODO` block before the PR merges; the per-surface doc bar
> in [ADR-0100](../../adr/0100-project-wide-doc-substance-rule.md) makes this page mandatory.

## Summary

TODO(@NAME@): one paragraph — what the tool does, who calls it, what the input/output contract is.

## Input schema

```jsonc
{
  "type": "object",
  "required": [],
  "properties": {
    // TODO(@NAME@): document each property + type + constraints.
  }
}
```

## Output schema

```jsonc
{
  // TODO(@NAME@): document the response payload (must match Go + Python byte-for-byte).
}
```

## Example invocation

```bash
# TODO(@NAME@): paste a real MCP `tools/call` request + response trace.
```

## Error modes

| Error                          | Cause                                  | Resolution                         |
|--------------------------------|----------------------------------------|------------------------------------|
| `invalid arguments`            | TODO(@NAME@)                           | TODO(@NAME@)                       |
| `vmaf binary not found`        | TODO(@NAME@)                           | TODO(@NAME@)                       |

Every error path MUST set `isError=true` on the MCP response
(see [`project_mcp_iserror_must_be_true`](../../../docs/development/mcp-error-signalling.md)
if present, else this file is the authority).

## Security considerations

TODO(@NAME@): file-path validation, shell-injection avoidance, resource limits.

## References

- Go handler: [`cmd/vmafx-mcp/impl_@NAME@.go`](../../../cmd/vmafx-mcp/impl_@NAME@.go)
- Python handler: [`mcp-server/vmaf-mcp/src/vmaf_mcp/tools/@NAME@.py`](../../../mcp-server/vmaf-mcp/src/vmaf_mcp/tools/@NAME@.py)
- Parity contract: [ADR-0703](../../adr/0703-go-grpc-scoring-service.md)
